# Create a new directory and enter it
function mk() {
    mkdir -p "$@" && cd "$@"
}

function gitNewBranchOnMaster() {
    git checkout master &&
    git fetch && git pull &&
    git checkout -b "$1";
}
